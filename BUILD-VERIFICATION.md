# Build verification

## Reliable panel details and clearing optional captions — 14 September 2026

- Replaced Framing and Angle text datalists with native select menus that always expose every preset, **Not set**, and **Custom value…**. Existing custom strings remain editable and independent per panel. Opening Custom reflects the current saved value; deleting its text keeps the editor available for retyping. UI-only option values are never saved as metadata.
- **Clear panel details** atomically clears the selected panel's name, framing and angle. Explicit clearing also resets transient custom editors. The action has its own Undo boundary. Dialogue, direction, image versions, full-width preference and neighbouring panels remain unchanged. Blank PDF panel names now omit the old “Untitled panel” fallback while retaining the frame identifier.
- **200 tests pass**, including mounted preset/custom/navigation/persistence/clear/Undo regressions and a PDF check across all three layouts. Typecheck, lint, scoped formatting and the production build pass. The existing PDF-library bundle-size warning remains.
- Live production review at **1280×800** verified preset changes from Medium close-up, custom draft initialization, independent custom values across panels, custom text editing and deletion, atomic Clear, two separate Undo steps, explicit Save and reload. The saved folder manifest confirms every non-detail field, the neighbouring panel and image asset metadata are unchanged. Both pages of its PDF preview retain direction and dialogue, show only the identifier for the cleared panel, and retain the neighbour's custom labels.
- At **507px**, the panel-details section's client and scroll widths both measure **475px**, with no horizontal page overflow. Custom inputs and Clear remain usable. No browser console errors appeared. The disposable project was closed through the app, Teaser restored, viewport settings reset and the temporary review tab closed.
- The app was rebuilt and restarted with zero active image requests and the existing **34-attempt** ledger retained. It remains at http://127.0.0.1:4317/. No paid generation or usage reset was required. Final review: **8.9/10, pass**, after one correction round for custom-editor state.

## PDF editorial layout and print optimization — 14 September 2026

- PDF shot headings now spell out **Scene 01, Shot 01**. The code beneath a frame is **S01-01**, with A/B suffixes only for multiple panels. The first-page title is smaller, subsequent project headers are quiet, and the footer retains **Page X of Y**. CAMERA, ACTION and DIALOGUE bodies stack beneath their headings for large frames; dialogue retains embedded Courier Prime.
- Short dotted continuation cues replace page-spanning wrap arrows. Every drawn cue includes both endpoint codes, its transition/movement type and complete description. Outgoing repeats appear after the source, incoming repeats before the destination, with centered text, extra row spacing and light dividers. When a full source page cannot hold its repeat, the complete incoming cue remains with the destination. Very long flowing destinations may retain only the source repeat; a final guard ensures these two fallbacks can never suppress both copies.
- Large frames and short direction are measured together. Artwork shrinks only as needed for the available body space, keeps its configured ratio, and remains centered with aligned captions. The eight explicit large-frame choices in the real project are retained. Long copy continues at readable type sizes.
- Added isolated artwork embedding and image-encoding modules. Identical selected sources share one PDF image; each panel retains its own transform. Raster-capable export downsamples to about 200 DPI, uses JPEG quality 0.88 and processes images sequentially. Small native files can pass through, and unsupported raster decoding preserves readable native PNG/JPEG bytes. Original files and unused image versions are unchanged; no new app dependency was added.
- Reviewed the matching Desktop PDF: **67,422,661 bytes**, ten pages, 31 placed images. Image streams account for 99.88% of its size; repeated images account for about 7.1 MB. The same 31-frame project now produces a **2,561,877-byte portrait test export** (about **96.2% smaller**), **3,654,772-byte landscape two-column** and **3,386,054-byte landscape three-column** exports. Size checks use a raster-capable QA adapter; native browser previews also render the real source PNGs successfully.
- All **42 final rendered pages** were visually reviewed: 11 portrait, 16 landscape two-column and 15 landscape three-column. All 31 artwork placements, all 49 authored printed fields at their required occurrence counts, all six connection notes and every page footer are retained. No off-page text, blank/title-only pages, isolated short dialogue or cue-only pages occur in this real project. The last defensive cue fix leaves the reviewed text and artwork geometry unchanged in all three layouts.
- **197 tests pass**, along with typecheck, lint, formatting and the production build. New coverage includes image reuse and transformations, raster sizing/fallback/error paths, independent source preservation, stacked full-width fields, repeated cue placement, first-page/scene heading boundaries, short wide-frame grouping and transition preservation before long flowing dialogue. Tests distinguish an optional repeated cue from the required complete connection content. The existing PDF-library chunk-size warning remains.
- Production review at **1280×800** verified all three complete-project previews, page navigation and Fit page/Fit width. At **507px**, the dialog's client and scroll widths both measure **465px**, with no horizontal page overflow. No app console errors appeared. The disposable review project was closed through the app, Teaser restored, temporary viewport overrides reset and the temporary development tab/server closed.
- Idle restart preserved the **34-attempt** ledger, with zero active generation and no configured session key. The rebuilt app remains at http://127.0.0.1:4317/. No paid generation, publishing or usage reset was required. Final review: **8.9/10, pass**, after four correction rounds.

## Camera movement labels and PDF row flow — 14 September 2026

- Camera movement options and timeline summaries begin with a capital letter. **Aerial** is available between panels and across shot cuts, using the same canonical movement list. Stored values remain stable lower-case identifiers; authored descriptions retain their exact casing. Validation, browser/folder saves, backups, splitting shots and agent handoff accept the new value. Timeline motion remains separate from single-frame image prompts.
- PDF movement/transition descriptions are centered line by line using font metrics, both beneath short arrows and in longer cue bands. Same-row connections retain solid arrows. Authored connections across rows or pages receive paired dotted arrows anchored to the actual frame midlines: out from the source's right edge, in toward the destination's left edge. Both stop within a 20-point paper margin and remain outside artwork. Quiet default cuts and stationary connections without notes stay unobtrusive.
- Initial review scored **7.9/10** because the wrap marks were detached from the frames and one description path remained left-aligned. Correction one aligned the prose and recorded actual frame placements, but its clamped arrows encroached on image borders (**8.2/10**). Correction two moved the dotted spans strictly outside the artwork and added PDF geometry regressions using the rendered frame bounds.
- **182 tests pass**, along with typecheck, lint and the final production build. Coverage includes both movement selectors, exact authored description casing, save/backup/folder round trips, centered text, portrait and capped landscape frames, wrapped shot transitions and independent movement across a cut. The existing PDF-library chunk-size warning remains.
- Root visually reviewed **26 rendered pages across nine PDFs**: 17 pages from a movement/full-width/long-note study, five from consecutive single-panel shots and four from a read-only snapshot of Quokka Scene 1. All three layouts retain every authored panel field and cue description exactly once at its expected occurrence count. All page footers are correct and extraction found no off-page text. The final arrow correction preserves pagination.
- Live production review at **1280×800** verified Aerial in both connection editors, exact lower-case descriptions after save/reload, all three PDF layouts and page navigation. Both connection dialogs and the PDF preview fit at **507px**; the preview's client and scroll widths are both 465px. No app console errors appeared. The isolated fixture was closed through the UI, Teaser restored, viewport settings reset, and temporary fixture/artwork snapshots/renders removed.
- The idle restart preserved the **34-attempt** ledger, with no configured session key and no active image request. The rebuilt app remains at http://127.0.0.1:4317/. No paid generation, publishing or usage reset was required. Final review: **8.9/10, pass** after two correction rounds.

## Scene-qualified labels and deliberate PDF sizing — 13 September 2026

- Added a shared display formatter: `SC03 · SH04` for a shot or its only panel, and `SC03 · SH04 · A/B` for multiple drawings. Selected editor headings, PDF headings, continuations and connection endpoints use the full code. Timeline strips use compact SH04 or A/B codes with full accessible labels. Custom titles and UUIDs remain intact; display numbers continue to follow draft order. Research and the distinction from locked production codes are recorded in `NAMING-CONVENTIONS.md`.
- Added **Panel details → Full-width in PDF**, independently saved per panel. New and legacy panels default to small artwork unless explicitly selected. The choice survives browser/folder saves, backups, import, duplication and undo. Long direction can flow without automatically enlarging an unchecked image. PDF footers now read **Page X of Y**.
- Review corrections kept short large-frame captions with artwork, unified the actual target-row height used by incoming movement cues, retained text for very long captions, and replaced fixed panel-tab label offsets with a measured-width grid. The singleton code/title gap is 10px in the production browser.
- **179 tests pass**, including independent checkbox persistence, legacy defaults, folder/backup round trips, naming, mixed sizes, dense page boundaries and 180-token long-caption fidelity. Typecheck and lint pass. The final CSS-only spacing adjustment also passed its focused editor check, typecheck, lint and formatting. The final production build passes, retaining the existing PDF-library chunk-size warning.
- Root visually reviewed 24 rendered pages across all three layouts: 14 pages from the mixed-size synthetic study and 10 from a read-only snapshot of Quokka Scene 4. All 13 Quokka panels, 16 authored description/action/dialogue fields, and the single dolly cue survive. Its portrait export has three pages, landscape two-column four, and landscape three-column three. Every page has the requested footer and no off-page text. Correction-round-two PNGs exactly match the reviewed visual layout while the new boundary regressions cover the repaired cases.
- Live review at 1280×800 verified legacy-small default, a saved full-width choice after reload, undo, independent B/C settings, all three PDF layouts and page navigation. At 507px the editor and preview stay within the viewport; the modal has equal client/scroll widths of 465px. No app console errors appeared. The test project was closed through the UI, Teaser restored, viewport settings reset, and temporary artwork snapshots/renders removed. The local app remains at http://127.0.0.1:4317/; idle restarts preserved the 34-attempt ledger. No paid generation, publishing or usage reset was required. Final review: **8.9/10, pass** after three correction rounds.

## PDF visual design polish — 12 September 2026

- Simplified the opening masthead to one project title and separator, normalized scene/shot identification, and suppressed only generated titles whose numbers match their displayed position. Custom titles remain intact. Fresh-page headings clear the running-header rule, and completed shots reserve space before the next heading.
- Character cues stay with the first line of dialogue across page breaks. PDF dialogue retains embedded Courier Prime, with sans-serif section labels. Paired single-frame shots now measure and align headings, captions and direction against the centered artwork width, including aspect-capped landscape frames.
- Brief two-column camera notes sit below their arrows when the actual gutter and frame height permit. Longer notes use aligned endpoint-labelled bands. Measurement and drawing share the same placement predicate, preventing duplicated notes and caption collisions. Three-column density and return-arrow behavior remain covered.
- **161 tests pass**, along with typecheck, lint and the final production build. New regression coverage includes dialogue cue pagination, compact title fit, inline and fallback note location/exact-once output, inline shot connections without overlapping bands, and capped-frame alignment. The existing PDF-library chunk-size warning remains.
- Root rendered the saved Quokka Scene 4 using a temporary read-only snapshot of its actual artwork: 13 panels, 16 authored action/description/dialogue fields, and its dolly cue are preserved in all three layouts. Portrait has three pages, landscape two-column has five, and landscape three-column has three. All 11 rendered pages were visually reviewed; extraction found no off-page characters or text crossing full-width rules. The final alignment change affected only landscape two-column page one; the other ten PNGs matched the inspected renders byte-for-byte.
- Root reviewed live production previews at 1280×800, including the six-frame landscape page, all three layout choices and page navigation. The 507px dialog has no horizontal overflow. No browser console errors appeared. Temporary project snapshots and review renders were removed after inspection; the reusable snapshot renderer is retained in `studio/tests/render-project-review-pdfs.mjs`. Idle restarts preserved the 34-attempt generation ledger; no paid generation, publishing or usage reset was used. Final review: **8.9/10, pass**.

## Courier PDF dialogue and adaptive single-frame shots — 12 September 2026

- PDF character names and dialogue body now use embedded Courier Prime. The `DIALOGUE` heading, panel labels and general copy retain Noto Sans. Font metrics are used for wrapping in compact rows, full-width frames and continuation pages. The 71,188-byte font and its SIL Open Font License are bundled locally with provenance recorded in `studio/lib/pdf/fonts/README.md`.
- Concise consecutive single-panel shots share two- or three-column rows, including ordinary cuts. Each keeps its shot number, title and panel direction. An unpaired concise shot can shrink to fit a page already containing frames; a measured lookahead can also make room to start the following multi-panel shot. Quiet boundaries allow this adaptation; authored connections retain their established placement. Isolated shots retain the roomy treatment, and long copy can continue onto additional pages.
- Two correction rounds addressed exact gutter measurement, static camera notes, square/custom frame widths, compact title fitting, odd final shots, fresh-page heading clearance and the lookahead's downstream page guard. Tests exercise compact and continued Courier text, three-shot movement notes, narrow-gutter fallback, square copy/title fallback and custom wide-ratio pagination.
- **154 tests pass**, with typecheck, lint and production build passing. Root independently extracted all 65 lines of the continued-dialogue fixture exactly once in order, including smart punctuation, and verified Courier Prime character names with Noto Sans dialogue labels. All inspected QA PDF pages have zero off-page characters.
- Root inspected the generated portrait and landscape PDFs, including compact odd shots, a compact singleton before a multi-panel shot, roomy frames and continuation pages. The actual three-shot artwork fixture now uses one portrait page and one landscape three-column page; the previous live landscape export used four pages. The existing six-panel action/dialogue/movement fixture still fits one landscape three-column page.
- The rebuilt production app was reviewed at 1280×800: all three layouts render, the portrait and three-column samples show one page, two-column page navigation works, and Courier is visible in the preview. The 507px dialog has no horizontal overflow. No browser console errors appeared.
- The separate Courier review project was closed through the UI, its folder and image retained under `/private/tmp`, Teaser restored and the temporary viewport reset. The app is running at http://127.0.0.1:4317/. The idle restart preserved the existing 34-attempt ledger; no paid generation, publishing or usage reset was used. Final review: **8.8/10, pass**.

## Dialogue typography and print layouts — 12 September 2026

- The dialogue editor uses the installed Courier New font, with Courier and monospace fallbacks. Production browser review confirmed the computed font and availability; the portable PDF retains its bundled Noto Sans fonts.
- PDF preview offers A4 portrait with two frames across, landscape with two, and landscape with three. Image-to-panel-label spacing is now about 10 points in compact rows, with captions aligned to the actual image frame. Portrait supports three compact rows when copy permits. Landscape supports two; long copy receives additional pages rather than smaller text.
- Same-row camera moves have labelled arrows. Short three-column movement notes sit in the gutters; longer notes retain a full-width fallback. A return arrow joins rows, identifying both endpoints. Page-breaking cues stay with their destination. Tests cover long cues, trailing transitions, legacy camera deduplication, odd panel counts and long landscape titles without copy loss.
- Root reviewed four PDF revision passes. Corrections addressed empty bands, premature page breaks, header-rule collisions, caption alignment, long-title pagination and representative action/dialogue density. The six-panel production fixture, including six action lines, dialogue on C and five camera moves, fits one landscape three-column page. Artwork height is capped at 80 points in this mode; text sizes are preserved. The seven-panel fixture puts four frames on the first landscape two-column page and six on the three-column page.
- **139 tests pass**, along with typecheck, lint and the final production build. A timing-dependent folder-close test was corrected: its simulated disk failure now remains active through both autosave and close, then explicitly recovers. It still verifies preserved dialogue, no folder close on failed save, and a successful subsequent save.
- Root rendered and inspected every page of the three layout QA PDFs, plus `output/pdf/storyboard-six-panel-dialogue-review.pdf`. Live production review at 1280×800 verified Courier New, all three layout selections, page navigation, retained layout while toggling production notes, and the six-frame single-page preview. The 507px modal remains usable with no horizontal overflow. No browser console errors appeared.
- The isolated review folder was closed through the UI, its files retained under `/private/tmp`, Teaser restored and the temporary viewport reset. The final local build runs at http://127.0.0.1:4317/. Rebuilds occurred with zero active image requests; the existing 34-attempt ledger is unchanged. No paid generation, reset, publishing or cloud service was used.

## Image framing and folder projects — 12 September 2026

- Added per-image-version Fit/Crop-to-fill, 10–400% scale, horizontal/vertical positioning, direct dragging, flips, Reset and Undo. Originals remain unchanged. An expandable Image framing bar keeps the controls available without permanently pushing dialogue down in a MacBook-sized window.
- Canvas artwork, aspect-correct thumbnails and PDF export share the same placement geometry. PDF clipping and flips preserve the frame border and composition guides. Selected previous-panel references and API revision inputs receive temporary framed PNGs; manual generation packs use the same rendering while retaining the source originals.
- New projects can create a named folder beneath a chosen parent. Existing browser projects can use Project settings → Save project to folder. Editable metadata lives in `project.storyboard.json`; references and every image version live as ordinary content-addressed files in `images/`. Metadata autosaves avoid re-uploading unchanged files. Save, Save now and Command-S are available. Folder artwork is exempt from the browser-only 2 GiB working limit; per-image limits and available memory still apply.
- Folder persistence uses immutable assets, atomic metadata replacement, revision plus manifest-digest conflict detection, path/symlink validation, missing-file diagnostics and a remembered local registry. Close saves dirty work first and preserves the folder. Failed saves keep the project open; failed folder operations do not poison later saves; reopening in the same editor clears stale deletion/history guards. Browser recovery copies remain compatible and do not resurface after a folder project is closed.
- The generation server writes successful results into an open project's folder before returning the response. Mocked provider tests cover successful persistence and folder-write failure without discarding the completed image or recovery result. No paid image call was used for validation.
- **128 tests pass**, along with typecheck, lint and the final production build. The mounted harness now keeps asynchronous database reads/writes inside React `act`, resolving transition-timing failures without suppressing console errors. Focused regressions cover framing, generated/reference pixels and ZIP bytes, folder assets, conflicts, browser migration, immediate-close save failures, failed-operation recovery and same-mount reopening.
- Root created a separate review project through the production UI in `/private/tmp/storyboard-folder-review-2026-09-12/Folder workflow review`, imported PNG and WebP fixtures, adjusted one version, verified direct dragging, reset/undo, version independence, explicit Save, Command-S, reload, Close, Open and server-restart persistence. Both saved image files match the original fixture bytes and their filenames' hashes. The 3.7 KiB manifest contains image tokens rather than embedded payloads. Closing leaves the manifest and both files on disk.
- Root visually inspected both pages of `output/pdf/storyboard-image-framing-review.pdf` and the live one-page export from the folder project. Cropping, offset, horizontal/vertical flips and printed dialogue are correct. The 1280×800 and 507px layouts were reviewed; no horizontal overflow or app console errors appeared. The native macOS picker was invoked but could not be fully operated by the available desktop automation; the typed-path workflow was verified end to end.
- The review folder was closed through the UI, Teaser selection restored, and temporary viewport settings reset. The local app remains at http://127.0.0.1:4317/. Restarts were performed with zero active image requests; all 34 existing generation attempts remain in the ledger. The session-only API key was cleared. No weekly reset, publishing or cloud storage was used.
- Remaining limits: the editor still loads project artwork into memory, portable archives retain their 2 GiB bound, and unused image files are retained for recovery. Atomic file replacement is not a promise of durability against every power-loss scenario; same-user filesystem path races cannot be fully excluded by portable Node filesystem APIs.

## Rough thumbnails and larger projects — 10 September 2026

- New rough style targets a one-to-two-minute thumbnail by a professional storyboard artist: confident gesture and staging, primitive shapes, faceless figures, sparse backgrounds and no polished detail. Detailed references and revision inputs are explicitly simplified. The previous default rough-pencil names receive these instructions without rewriting existing artwork or custom styles; project settings offers the new preset.
- Raised the working project limit from 96 MiB to 2 GiB. IndexedDB v3 separates immutable, deduplicated image assets from metadata, migrates embedded v2 records on their next atomic save, and retains revision checks and deletion tombstones. Metadata autosaves check asset keys and do not rewrite existing image payloads.
- Added portable `.storyboard` ZIP backups with bounded individual entries and hash verification. Export/import limits agree; legacy JSON imports remain supported up to 512 MiB. UI accepts the new format, indicates backup preparation, and distinguishes the project limit from origin-wide browser quota.
- **109 tests pass**; focused prompt tests also pass after the professional-thumbnail wording refinement. Typecheck and lint pass. A separate synthetic Node/fake-IndexedDB check saved, edited, loaded, backed up and restored 300 distinct 512 KiB image payloads (200.1 MiB logical project), preserving every byte. Across initial and metadata saves only 300 asset writes occurred. Initial save 501 ms, metadata edit 139 ms, load 209 ms, backup 781 ms, import 3814 ms on this machine.
- Production build passes. Root checked project settings at 1280×800, applied the new rough preset to the current Chrome project and IAB Teaser, and confirmed the professional-thumbnail instruction in the assembled prompt. The Chrome project reopened after its image-backed migration with all visible artwork loaded, its three selected-panel versions present and exact dialogue retained. No app console errors appeared; Chrome reported only an unrelated extension error.
- The local app is running at http://127.0.0.1:4317/. Restart occurred with zero active requests and six existing generation attempts retained. The session API key was cleared by restart; no paid generation or usage reset was performed.
- The stress check is not a browser rendering benchmark or proof of practical 2 GiB operation. The editor still loads artwork into memory; archives retain their final Blob/decoded strings in memory. Removed assets currently remain in IndexedDB and consume quota. These limitations are documented in README. Server recovery copies remain separate, capped at the newest 25 files and 250 MiB in `studio/.local-data/openai-results`.

## Visible panel deletion — 10 September 2026

- Moved panel deletion out of collapsed details into a labelled control beside the panel strip, outside its horizontal scrolling area. Confirmation names the selected panel. Deleting selects the next available neighbour; deleting the final panel retains the empty shot and its Add panel action.
- Deletion removes only the selected panel and its endpoint connections. Undo restores artwork, direction and connections. Added two mounted regressions covering cancellation, selection, unrelated data, restoration and empty-shot behavior.
- **106 tests pass**. Typecheck, lint and production build pass. Root verified the visible control at 1280×800 and 507px, deleted the sandbox's last panel, confirmed its shot remained, and undid the deletion to restore both image versions and dialogue. Sandbox content was restored.
- The rebuilt local app runs at http://127.0.0.1:4317/. Restart occurred with zero active image requests and no configured key; no paid generation or usage reset was used.

## Timeline connections and dragging — 10 September 2026

- Added editable connections between shots and between panels. Shot connections support an editorial transition and an independent camera movement, with separate descriptions. Panel connections describe movement between moments. Pair IDs retain cues without retargeting them when items are reordered; restored adjacency restores the saved connection.
- Both strips have native drag handles, directional insertion feedback, earlier/later alternatives, stable selection and one-edit undo. Structural changes preserve older direction, prune deleted endpoints, remap duplicated panel connections and transfer split-shot boundaries. Existing unbound ending cues survive materialization and attach when their next shot is added.
- PDF short pairs place a labelled arrow physically between images, including combined Match cut + Dolly. Authored descriptions appear once; longer copy uses full-width frames and continuation labels. Ordinary cuts remain implicit. Root reviewed both pages of the realistic connection export after removing duplicate labels and improving long-cue spacing.
- **104 tests pass**, including connection validation, migration, clone/split, pair identity through reorder/undo, connection dialogs, unbound ending-cue append/undo, PDF fidelity, and agent metadata. Typecheck, lint and production build pass.
- Root verified actual native shot and panel dragging in the browser at 1280×800: order changed, selected artwork remained selected, reversed pairs defaulted cleanly, and undo restored the cue. Combined transition/movement editing and two-page PDF preview passed. The 507px dialog fits; the final timeline label wraps to show both Match cut and Dolly. Review edits were undone and the sandbox restored.
- Final review: **8.8/10**, passing after focused corrections for migration, PDF hierarchy and timeline readability. Remaining layout tradeoff: complex or lengthy connections use stacked full-width frames instead of forced horizontal pairs.
- The server was restarted only while idle. It runs at http://127.0.0.1:4317/ with the three existing usage records retained. No paid image calls, credential persistence, publishing or usage reset were performed.

## Independent panel direction — 10 September 2026

- Context, dialogue, camera movement, action, production notes, outgoing transitions, and reference selections now belong to each panel. Shot and scene labels remain organisational. New panels start with blank direction and no selected artwork; duplication preserves an independent copy.
- Older projects and backups preserve their shared text and reference choices by materialising them as panel values. Explicit empty strings and empty selections are preserved. Missing transitions retain a positional default: Continue inside a shot, the legacy outgoing transition at its end.
- Prompts, API payloads, generation packs and agent handoff metadata resolve the selected panel. Reference library deletion cleans both panel selections and legacy fields. PDF export uses each panel's dialogue/direction and omits obsolete shared shot context.
- All 89 tests pass, including legacy migration, cleared values surviving later edits/reload, independent fields across two panels, panel reference choices, exact long-dialogue PDF preservation and panel continuation labels. Typecheck and lint pass. Root's 1280×800 browser review on an isolated local preview confirmed differing camera/context/dialogue values survive navigation and reload. The browser automation's empty-string fill did not clear the control, so explicit-empty verification uses the passing mounted input-event regression.
- Root inspected all four pages of `output/pdf/storyboard-panel-details-review.pdf`: readable paired columns, uncut long direction, clear panel continuation labels, and the outgoing transition at the following scene. Coordinate/text checks found no off-page characters and retained all terminal long-text tokens exactly once.
- Production build passed. The app was restarted at port 4317 with no active generation; its three existing usage entries remain intact. The session API key was cleared by the restart, and Big Trimpy was told to re-enter it. Final production browser checks showed migrated legacy text in panel controls and a working two-page PDF preview. Temporary development server 4318 was stopped. No paid generation was made for testing.

## Previous-panel image references — 10 September 2026

- Added an optional per-panel thumbnail picker in Generate and Prompt. Earlier panels across the current scene are available; the exact selected image version is pinned without copying its bytes into project metadata. The API and manual generation packs include that image alongside selected library artwork.
- Domain checks cover chronology, pinned versions, validation/backups, shot duplication, deterministic prompt/input ordering, and exact ZIP bytes. Mounted checks cover persistence, submitted image bytes, unavailable references, input limits, and matching agent handoff metadata. Missing images and more than ten total API inputs are blocked before a paid request.
- Typecheck, lint and production build pass. Root reviewed the actual production app at 1280×800 and 507px, selected Panel 1 for a temporary Panel 2, confirmed the combined reference count and prompt filename, and verified the checked selection after reload. The temporary panel was removed, Teaser selection restored, and viewport override reset.
- The full 84-test run passed 83 tests and identified one outdated expected display label in the new test. After correcting that assertion, both new mounted tests passed in a focused rerun; production code was unchanged.
- The local server was restarted with no active request and no configured session key. The existing two usage records remain intact; no paid generation was used for this review. Actual generated continuity remains dependent on the image model and the supplied direction.

## Frame direction, quality, and PDF motion update — 10 September 2026

- New panels start with their own blank Frame direction, visible in Direction and Generate. A new image cannot silently reuse shared shot context. Existing descriptions remain intact, and an explicit reuse button can copy context into the selected frame without generating. Prompts prioritize that frame over shared context and nearby panels.
- Medium is the default, with Low and High alternatives. Quality remains selected across panel navigation; new version labels and usage rows identify the quality setting. The two existing Low requests measured approximately $0.0038 each. No additional paid calls were made to benchmark Medium or High.
- Added explicit outgoing Cut/Dissolve/Fade/Wipe/Match cut data and controls. Backups retain the optional transition field, and legacy shots imply Cut at real editorial boundaries.
- PDF exports now carry authored camera/action copy on continuity connectors and show transitions between shots/scenes. Long connector summaries defer to complete paginated directions; transition labels stay with the following shot. Production notes are optional and off by default. Redundant panel titles and generation/style metadata are omitted; frame ratios retain up to three decimals.
- **76 tests pass**, including mounted blank-frame/quality/reuse coverage and long-motion/transition/PDF fidelity cases. Full typecheck, lint, and production build pass. Root reviewed the generated two-page motion study using the existing island images, plus the actual app at 1280×800. The 507px PDF controls also fit and remain usable.
- Browser checks verified the default Medium setting, blank new panel and retained High selection, preserved shared context, transition control, successful PDF regeneration with production notes toggled, and the saved state after undoing the sandbox edit. Temporary viewport settings and export options were reset; Teaser selection was restored.
- The server restart was performed with no active generation. The two existing usage records remained available. The session-only API key must be entered again; no key was read or persisted by the assistant. The updated server runs at http://127.0.0.1:4317/.

## OpenAI generation update — 10 September 2026

- Added explicit single-image generation/revision through local OpenAI routes, automatic selected-reference uploads, cinema-compatible output presets, local-memory key setup, a persistent usage ledger, and retained-image recovery.
- **69 tests pass**, including 12 mocked proxy tests, five mounted generation-flow tests, three API prompt-mapping tests, and five pricing tests. Full typecheck, authored-code lint, and production build pass. Lint excludes temporary mounted-test bundles so concurrent checks do not inspect generated fixtures.
- Mocked transport checks verify actual multipart reference bytes and revision order, single-request locking across route service instances, request-ID deduplication, uncertain timeouts without retries, restart recovery, ledger pruning totals, valid-image recovery with a corrupt ledger, and malformed provider image rejection. No mock was used for a paid request.
- Mounted editor tests verify key configuration never generates or enters project backups; generated images return to the originating panel after navigation; dialogue and later edits survive; deleted/conflicted destinations offer image downloads; and orphaned attempts do not permanently block the UI.
- Root reviewed the production Generate interface at 1280×800, including the no-key setup, cinema default, selected reference count, revision mode, collapsed pricing detail, and persistent-ledger wording. At 507px, controls remain available and the page width equals the viewport width. Temporary viewport overrides were reset and Teaser selection restored.
- Production HTTP checks: status returns 200, foreign-origin mutation returns 403, and an unconfigured generation returns 401 without contacting OpenAI. Final status reports no configured key and zero attempts. The client JavaScript bundle contains none of the server key variable, environment key name, ledger filename, or provider request endpoints checked.
- Final local server runs at http://127.0.0.1:4317/. Usage/output data live under `studio/.local-data/`; credentials are absent from browser project storage and API response payloads. Optional environment setup is documented in `README.md` and `studio/.env.example`.

Actual provider acceptance, generated-image quality, character consistency, latency, and per-frame cost still need a real scene trial with Big Trimpy's API key and artwork. No paid API call was made. Big Trimpy manually reset usage during development; the assistant did not redeem another reset.

## Reference library update — 10 September 2026

- Added project-wide named reference groups, multiple labelled views, continuity notes, shot-wide selection, and portable generation/revision ZIP packs.
- 44 tests pass, including actual ZIP payload checks, group selection through panel changes, later-view propagation, deletion/undo, no-scene uploads, legacy artwork grouping, and matching WebMCP reference metadata. Typecheck, lint and production build pass.
- Root reviewed the library at 1280×800, uploaded two real PNG fixtures in the in-app browser, named their views, selected their character for a shot, and confirmed both files and notes in the prompt after reload. The pack action produced its retained download link. No app console errors were observed.
- An independent Python ZIP reader validated CRCs, exact source-image bytes, and prompt-to-filename mapping. Chrome automated uploads were unavailable because its extension lacks file-URL permission; this does not establish a defect in manual uploads. Native Chrome PDF/backup downloads were verified in the earlier review below.
- The library is scoped to each project, shared across all its scenes and panels. No image API was connected or charged. Test artwork remains in explicitly named review projects; the user's Teaser content was preserved.

## Transition cues and shot boundaries — 10 September 2026

- Added optional independent panel transition notes. Cuts start new shots within the same scene; the editor can split after a panel, preserving following artwork, panel identifiers, references and direction in one undoable edit. Existing boards are not silently reorganized.
- Ordinary cuts without notes are implicit in PDF exports. Special transitions and authored cues receive a labelled boundary marker; explicit final cues retain an `AFTER` panel label. Notes paginate in full and stay out of image-generation prompts.
- **94 tests pass**, including split/undo, persistence, prompt exclusion, PDF cue fidelity and near-bottom pagination. Typecheck, lint and production build pass.
- Root reviewed the two-page sample and four-page stress PDF renders. All 150 stress cue tokens occur exactly once, with no off-page characters. Browser review at 1280×800 verified the note, split into two shots in one scene, preserved art/dialogue, successful two-page PDF preview and undo. The 507px controls fit. Sandbox edits were undone and Teaser selection restored.
- Restarted only with zero active image requests. The final server runs at http://127.0.0.1:4317/; all three prior usage records remain, with no configured API key and no new paid request.

## Earlier review

The bounded review loop passed at **8.8/10 after two revision rounds**. See `QUALITY-REVIEW.md` for rubric, earlier failures, evidence, and remaining limitations.

- 36 automated tests pass: domain validation, persistence conflicts/deletions, mounted editor behavior, download artifacts, imported-copy draft isolation, PDF fidelity/scaling, and launcher identity/readiness.
- Typecheck, authored-code lint, and production build pass. Unmodified scaffold UI primitives remain excluded from lint and included in typechecking.
- Actual browser review at 1280×800 verifies frame/dialogue layout, PNG import, WebP replacement/history restoration, references and prompts, custom aspect ratio, and PDF preview. The secondary 507px layout exposes all workspace actions.
- Actual Chrome downloads completed through macOS Save dialogs. Root validated the resulting JSON backup and two-page PDF from disk. Embedded-browser preview works; its native file delivery was not independently confirmed.
- PDF render/extraction QA: normal two-page and seven-page stress exports; all 80 dialogue IDs and 36 caption IDs exactly once, punctuation preserved, no text outside page/body margins.
- The 60 MiB benchmark uses real serialized data URLs in Node/fake-IndexedDB. It demonstrates reduced redundant serialization, not guaranteed browser latency.
- Read-only WebMCP registration and selected-panel prompt output verified in the embedded browser.
- Local launcher checks app identity, build freshness and readiness. Final server runs at http://127.0.0.1:4317/.

No paid image API call, publishing, cloud storage, or weekly reset was used. Actual generated-image quality and character consistency await a reference-art scene trial. Keep the same browser profile and use JSON backups to preserve editable work.
