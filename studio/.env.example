# Optional persistent server-side configuration.
# Copy this file to .env.local and enter your own key there, or use the app's
# connection panel for a key kept only until the local server stops.
# Never use a NEXT_PUBLIC_ or VITE_ prefix for this credential.
OPENAI_API_KEY=
